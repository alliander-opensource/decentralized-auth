server {
    listen       80;
    server_name  localhost;

    location / {
        root   /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    location /iota-bindings.emscripten.wasm {
        default_type application/wasm;
    }
}
