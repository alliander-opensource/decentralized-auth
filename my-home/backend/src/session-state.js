// Mapping from sessionId to: IOTA seed; IOTA address; MamClient; MAM (initial) root
// (Every user has its own MAM state)

const state = {
  // 'sessionId': {
  //   iotaSeed: xx,
  //   iotaAddress: xx,
  //   mamClient: xx,
  //   mamRoot: xx,
  // },
};

module.exports = state;
